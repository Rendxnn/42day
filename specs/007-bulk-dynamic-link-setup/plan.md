# Plan

La migración reserva la tabla de idempotencia y enlaza los eventos de auditoría al lote. La RPC y la UI no se mezclan con el modelo de perfiles: la operación recibe un target previamente validado.
